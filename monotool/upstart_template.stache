#
# Generated by monotool.{{ monotool_version}} on {{ timestamp }}
# symlink to me from /etc/init/{{ project_name }}
#

description "Upstart for {{ project_name }}"

start on runlevel [2345]
stop on [!2345]

DIR="$(dirname "$(readlink -f "$0")")"
chdir $DIR/{{ output_path }}

exec /usr/bin/mono --server --debug {{ project_name }}.exe >> /var/log/{{ project_name }}.log
